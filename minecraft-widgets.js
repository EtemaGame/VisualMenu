// VisualMenu Studio - Specific Minecraft Widget Definitions

const MC_WIDGETS = {
    button: {
        defaultWidth: 200,
        defaultHeight: 20,
        render: (ctx, w) => {
            // Placeholder for canvas-based rendering if we move away from DOM
        }
    },
    slot: {
        defaultWidth: 18,
        defaultHeight: 18,
    },
    label: {
        defaultWidth: 100,
        defaultHeight: 10,
    }
};

console.log("Minecraft Widgets Library Loaded");
