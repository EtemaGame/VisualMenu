// Preload script for VisualMenu Studio
// In the future, we can add bridge functions to read/write local files here.
window.addEventListener('DOMContentLoaded', () => {
    console.log("VisualMenu Desktop Bridge Active");
});
